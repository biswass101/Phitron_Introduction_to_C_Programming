#include<stdio.h>
int main()
{
    // int rahim, karim;
    // char c;
    // float f;
    // scanf("%d %f %c", &rahim, &f, &c);
    // printf("%d %0.2f %c", rahim, f, c);


    //fomatted input-->
    // 10% 20% like this

    int a, b;
    scanf("%d%% %d%%", &a, &b);
    printf("%d%% %d%%", a, b);
    return 0;
}